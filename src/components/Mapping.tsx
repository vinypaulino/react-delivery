import { Grid } from "@material-ui/core";

type Props = {
  
};

export const Mapping = (props: Props) => {
  return (
    <Grid container>
      <Grid item  >Formulario</Grid>
      <Grid item > Mapa </Grid>
    </Grid>
  );
};